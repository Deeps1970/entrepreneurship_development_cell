"use client";
import { Menu, X, ArrowUpRight } from "lucide-react";
import { useEffect, useState } from "react";
import { navItems } from "@/data/site";
export function Navigation() { const [open, setOpen] = useState(false); useEffect(() => { document.body.classList.toggle("menu-open", open); return () => document.body.classList.remove("menu-open"); }, [open]); return <><header className="nav"><a className="brand" href="#home" aria-label="EDC home">EDC<span>.</span></a><nav>{navItems.map(item => <a href={`#${item.id}`} key={item.id}>{item.label}</a>)}</nav><a className="join" href="#contact">JOIN EDC <ArrowUpRight size={14}/></a><button className="menu" onClick={() => setOpen(!open)} aria-label="Toggle navigation" aria-expanded={open}>{open ? <X/> : <Menu/>}</button></header><aside className={`mobile-nav ${open ? "open" : ""}`} aria-hidden={!open}><p>ENTREPRENEUR DEVELOPMENT CELL</p>{navItems.map((item,i) => <a onClick={() => setOpen(false)} style={{ transitionDelay: `${i * 55}ms` }} href={`#${item.id}`} key={item.id}>{item.label}</a>)}<a className="join" onClick={() => setOpen(false)} href="#contact">JOIN EDC <ArrowUpRight size={15}/></a><small>INSTAGRAM · LINKEDIN · YOUTUBE</small></aside></>; }
